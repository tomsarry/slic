#include "helpers.h"
#include "tests.h"

int main() {
  if (run_all_tests()) return 1;

  // float **m = allocate_float_matrix(10, 10);

  // m[5][5] = 10.5;
  // printf("is %f", m[5][5]);

  // free_float_matrix(m);
  return 0;
}